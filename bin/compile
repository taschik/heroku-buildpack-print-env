#!/usr/bin/env bash

echo 'env:'
env

echo 'envdir crazy_town env:'
mkdir crazy_town
for f in $3; do
  if [ -s $3/$f ]; then
    echo $(tr "\n" "\0" < $3/$f)
  else
    echo "\n"
  fi > crazy_town/$f
done
envdir crazy_town env
