#!/usr/bin/env bash
set -x
echo 'env:'
env

cp -r $3 $1/happy_town

echo 'envdir crazy_town env:'
mkdir -p $1/crazy_town
for f in $(ls $3); do
  if [ -s $3/$f ]; then
    echo "$(tr "\n" "\0" < $3/$f)"
  else
    echo "\n"
  fi > $1/crazy_town/$f
done
envdir $1/crazy_town env
